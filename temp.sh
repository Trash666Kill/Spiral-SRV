# Main function to orchestrate the setup
main() {
    update
    interface
    hostname
    target_user
    passwords
    packages
    directories
    trigger
    firewall
    mount
    ssh
    grub
    network
    later
    finish
}

# Execute main function
main